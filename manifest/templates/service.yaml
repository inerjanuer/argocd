apiVersion: v1
kind: Service
metadata:
  name: {{ include "arquitectura.fullname" . }}
  labels:
    {{- include "arquitectura.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arquitectura.selectorLabels" . | nindent 4 }}
