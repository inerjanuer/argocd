apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arquitectura.fullname" . }}-test-connection"
  labels:
    {{- include "arquitectura.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arquitectura.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
